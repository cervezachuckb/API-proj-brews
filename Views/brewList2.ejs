<!DOCTYPE html>
<html>

<head>
  <title>Express App</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
    }

    button {
      border: none;
      border-radius: 5px;
      color: white;
      cursor: pointer;
    }

    #noAuth {
      margin: 10px;
      padding: 15px 30px;
      font-size: 16px;
      background-color: #3498db;
    }

    #myForm {
      margin-top: 50px;
    }

    #page {
        background-color: blue;
        padding: 10px;
        font-size: 1rem;
        margin: 10px;
    }
    #page:hover {
        background-color: lightblue;
    }
    #page2 {
        float:right;
        background-color: red;
        padding: 10px;
        font-size: 1rem;
        margin: 10px;
        display:block;
    }
    #page2:hover {
        background-color: lightblue;
    }

    .response-area {
      width: 500px;
      border-radius: 15px;
      margin-top: 20px;
      background-color: white;
      padding: 20px;
    }

    #test {
      width: 100px;
      background-color: red;
      font-size: 1.25rem;
      border-radius: 20px;
      color: white;
      padding: 20px;
      display: block;
    }

    #test:hover {
      background-color: pink;
    }

    .homeBtn {
      color: white;
      background-color: green;
      padding: 10px;
      margin-top: 30px;
      font-size: 1.25rem;
    }

    .homeBtn:hover {
        background-color: yellow;
    }
  </style>
</head>

<body>
    <form action="/submit3" id="form" class="form" method="POST">
        <label for="idInput">Number of breweries per page:</label>
        <select name="perPage" class="form-select" onchange="this.form.submit()">
          <option value="select">select</option>
          <option value="5">5</option>
          <option value="10">10</option>
          <option value="15">15</option>
          <option value="20">20</option>
        </select>
    </form>
    
    <div class="response-area">
      <% if (locals.stateChoice) { %>
        <h2> List of breweries in <%= stateChoice %> ... </h2>
        <% } %>
        <% if (locals.stateChoice2) { %>
        <h2> List of breweries in <%= stateChoice2 %> (cont.)... </h2>
      <% } %>

    <p>
    <% if (locals.brewList) { %>
      <h2>You've come to the end of the list of breweries in <%= brewList %>. Select another state! 🍺 🍺 🍺</h2>
    <% } %>

    <% if (locals.brewByState) { %>
    <ul>
       <% brewByState.forEach((result) => { %> 
       <li>   <%= result.name %>, <%= result.address_1 %>, <%= result.city %>, <%= result.state %> </li>
          <% }) %>
   </ul>
    <% } %>

   <% if (locals.error) { %>
          <%= oops %>
    <% } %> 
    </p>

    <button type="submit" id="page" value="page" name="choice" onclick="window.location.href='/butt'">Next Page</button><button type="submit" id="page2" value="page" name="choice" onclick="window.location.href='/submit2'">Previous Page</button>

  </div>

  <button class="homeBtn" type="submit" onclick="window.location.href='/'">Go home bro.</button>
</body>

</html>