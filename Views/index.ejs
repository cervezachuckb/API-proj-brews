<!DOCTYPE html>
<html>

<head>
  <title>Express App</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
    }

    button {
      border: none;
      border-radius: 5px;
      color: white;
      cursor: pointer;
    }

    #noAuth {
      margin: 10px;
      padding: 15px 30px;
      font-size: 16px;
      background-color: #3498db;
    }

    #myForm {
      margin-top: 50px;
    }

    #page {
        background-color: blue;
        padding: 10px;
        font-size: 1rem;
        margin: 10px;
    }
    #page:hover {
        background-color: lightblue;
    }

    .randoBrewery {
      width: 500px;
      border-radius: 15px;
      margin-top: 20px;
      background-color: white;
      padding: 20px;
    }

    .response-area {
      width: 500px;
      border-radius: 15px;
      margin-top: 20px;
      background-color: white;
      padding: 20px;
      display: none;
    }

    #test {
      width: 100px;
      background-color: red;
      font-size: 1.25rem;
      border-radius: 20px;
      color: white;
      padding: 20px;
      display: block;
    }

    #test:hover {
      background-color: pink;
    }
  </style>
</head>

<body>
  <button type="submit" id="noAuth" onclick="window.location.href='/random'">Get A Random Brewery Name</button>

  <div class="randoBrewery">
    <p>
    <% if (locals.buttface) { %>
      <%= buttface %>
    <% } %>
    </p>
  </div>

  <form action="/submit" id="myForm" method="post">
    <label for="idInput">Type in a state name and see a list of breweries:</label>
    <input type="text" id="city" name="state" method="post">
    <input id="post" type="submit" value="Submit this!">
  </form>

  <div>
      <% if (locals.stateChoice) { %>
        <h2> List of breweries in <%= stateChoice %> ... </h2>
        <% } %>
        <% if (locals.stateChoice2) { %>
        <h2> List of breweries in <%= stateChoice2 %> (cont.)... </h2>
      <% } %>
  </div>

  <div class="response-area">
    <p>
    <!--  if (locals.stateChoice2) { %>
       stateChoice2 %>
     } %> -->

    <% if (locals.brewList) { %>
      <h2>You've come to the end of the list of breweries in</h2> <%= brewList %> <h2>. Select another state! 🍺 🍺 🍺</h2>
    <% } %>

    <% if (locals.brewByState) { %>
    <ul>
       <% brewByState.forEach((result) => { %> 
       <li>   <%= result.name %>, <%= result.address_1 %>, <%= result.city %>, <%= result.state %> </li>
          <% }) %>
   </ul>
    <% } %>

   <% if (locals.error) { %>
          <%= oops %>
    <% } %> 
    </p>

  </div>
    <!-- <button type="submit" id="test">Press Me Fucker!</button> -->
      <!--  if (locals.document) { %>
           const btn = document.getElementById('test'); %> 
       } %>
      <!--  btn.addEventListener('click', function handleClick() { %>
       btn.textContent = 'Button clicked'; %>
       }); %> -->
     <!-- <script>
       function myFunction() { %>
       document.getElementById("btn").style.display = "none"; %>  
       } %>
    </script> -->

</body>

</html>