<!DOCTYPE html>
<html>

<head>
    <title>Beer Hunter</title>
    <link rel="stylesheet" href="styles/main.css">
</head>

<body>
    <form action="/submit4" id="form" class="form" method="POST">
        <label for="idInput">Number of breweries per page:</label>
        <select name="perPage" class="form-select" onchange="this.form.submit()">
          <option value="select">select</option>
          <option value="5">5</option>
          <option value="10">10</option>
          <option value="15">15</option>
          <option value="20">20</option>
        </select>
    </form>

  <div class="response-area">
      <% if (locals.stateChoice) { %>
        <h2> List of breweries in <%= stateChoice %> ... </h2>
        <% } %>
        <% if (locals.stateChoice2) { %>
        <h2> List of breweries in <%= stateChoice2 %> (cont.)... </h2>
      <% } %>

    <p>
    <!-- < if (locals.brewList) { %>
      <h2>You've come to the end of the list of breweries in <= brewList %>. Select another city! 🍺 🍺 🍺</h2>
    < } %> -->

    <% if (locals.brewByState) { %>
    <ul>
       <% brewByState.forEach((result) => { %> 
       <li>   <%= result.name %>, <%= result.address_1 %>, <%= result.city %>, <%= result.state %> </li>
          <% }) %>
   </ul>
    <% } %>

   <% if (locals.error) { %>
          <%= oops %>
    <% } %> 
    </p>

    <!-- <button type="submit" id="page" value="page" name="choice" onclick="window.location.href='/butt2'">Next Page</button> -->
    
    <!-- this is testing out both state/city submits within butt route.-->
     <button type="submit" id="page" value="page" name="choice" onclick="window.location.href='/butt'">Next Page</button>

    <!-- <button type="submit" id="page2" value="page" name="choice" onclick="page2.display=none">Testes</button> -->

  </div>

  <button class="homeBtn" type="submit" onclick="window.location.href='/'">Go home bro.</button>
</body>

</html>